<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    {% load library %}

    <title>Best Endtimes Ever</title>
    <link rel="alternate" type="text/html" href="{% absoluteurl %}{% url home %}{% endabsoluteurl %}"/>
    <link rel="self" type="application/atom+xml" href="{% absoluteurl %}{% url home_feed %}{% endabsoluteurl %}"/>
    <id>tag:www.bestendtimesever.com,2008:blog</id>
    <generator>giraffe</generator>

    <updated>{% for action in actions %}{% if forloop.first %}{{ action.asset.updated|atomdate }}{% endif %}{% endfor %}</updated>

    {% if blogger %}
    <author>
        <name>{{ blogger.name }}</name>
        <uri>{{ blogger.openid }}</uri>
    </author>
    <rights>Copyright © 2009 {{ blogger.name }}</rights>
    {% endif %}

    {% for action in actions %}
        {% with action.asset as asset %}
        <entry>
            <title>{{ asset.title }}</title>
            <link rel="alternate" type="text/html" href="{% absoluteurl %}{{ asset.get_permalink_url }}{% endabsoluteurl %}"/>
            <published>{{ asset.published|atomdate }}</published>
            <updated>{{ asset.updated|atomdate }}</updated>
            <id>tag:www.bestendtimesever.com,2008:{{ asset.key }}</id>

            {% with action.person as blogger %}
            <author>
                <name>{{ blogger.name }}</name>
                <uri>{{ blogger.openid }}</uri>
            </author>
            {% endwith %}

            <content type="html" xml:base="{% absoluteurl %}/{% endabsoluteurl %}">
                {{ asset.content_as_html|escapexml }}
            </content>
        </entry>
        {% endwith %}
    {% endfor %}

</feed>
